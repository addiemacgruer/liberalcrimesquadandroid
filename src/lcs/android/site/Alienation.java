package lcs.android.site;

/** Who we've managed to annoy on site so far. Changed when we perform various Conservative acts.
 * Controls the CreatureTypes generated in encounters. */
public enum Alienation {
  /** Everyone! Good job. */
  ALL,
  /** Everyone but Liberals! */
  MODERATES,
  /** No-one! Let's get to work. */
  NONE
}